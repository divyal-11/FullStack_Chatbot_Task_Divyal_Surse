# Backend API URL — set this to your Railway backend URL after deployment
VITE_API_BASE_URL=https://your-backend.railway.app

# Admin token — must match ADMIN_TOKEN set on Railway
VITE_ADMIN_TOKEN=your_admin_token_here
